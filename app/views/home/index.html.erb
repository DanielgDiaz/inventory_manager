<% if notice %>
 <aside id="notice" class="small-container center-block my-5"><%= notice %></aside>
<% end %>

<h1 class='text-center'>Inventario</h1>

<ul class="catalog">
<% @articles.each do |article| %>

<div class="card-group m-3 px-2">
  <div class="card" style="width: 18rem;">
    <div class="card-body">
      <h5 class="card-title text-center"><%= article.title %></h5>
      <p> <%= article.category.name %> </p>
      <h6 class="card-subtitle mb-2 text-muted"><%= number_to_currency(article.price) %></h6>
      <p class="card-text"><%= truncate(strip_tags(article.description), length: 200) %></p>
      <hr/>
      <p class="card-subtitle mb-2"> Hay <b><%= article.quantity %></b> unidades, en <%= article.location.name %></p>

        <%= link_to 'Ver más...', article, class:"text-decoration-none " %>

      

      <%= link_to 'Retirar', edit_article_path(article), class:'btn btn-info' %>



</div>

<% end %>
</ul>